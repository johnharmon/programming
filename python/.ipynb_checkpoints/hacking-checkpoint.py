#!/bin/pyton3
import random
import math
import re 

def get_word_start_index(word):
    offset_range = 20 - len(word)

def generate_strings():
    rand = random.Random()
    str_array = '!@#$%^&*()[]{}_-+=<>,.?|'
    words = open('/usr/share/dict/words', 'r').readlines()
    used_words = []
    iteration_words = words.copy()
    for word in iteration_words: 
        if len(word) > 10:
            words.remove(word)
    word_indicies = rand.sample(len(words)-1, 10)

    password = rand.sample(len(word_indicies)+1, 1)

    random_words = ramdom.sample(range(0, len(words)), 10)
    word_len = 20
    for word in random_words:
        word_start = random.random(0,(20-len(word)
        leading_string = ''
        trailing_string = ''
        for index in range(0,(20-word_start)):
            




    
