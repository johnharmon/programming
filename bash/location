cd $(dirname $0)
workingdir=$(pwd)
location=$(dirname $0)/$(basename $0)
startchar=$(echo ${location:0:1})

#if [ $stion=$(echo ${location:1})
#
	fullpath="$workingdir/$(basename $0)"
	echo "fullpath is $fullpath"
#else
	
#	echo "fullpath is $location"
#fi
